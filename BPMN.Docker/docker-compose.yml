services:
  postgres:
    image: postgres:18
    env_file:
      - ../.env
    container_name: db
    restart: always
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql
      - ./migrations:/docker-entrypoint-initdb.d
volumes:
  pgdata: